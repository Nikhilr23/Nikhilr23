const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;requestAnimationFrame(()=>document.body.classList.add('hero-ready'));const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});reveals.forEach(el=>observer.observe(el));const progress=document.querySelector('.progress');addEventListener('scroll',()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max?scrollY/max*100:0}%`},{passive:true});const fine=matchMedia('(pointer:fine)').matches;const glow=document.querySelector('.cursor-glow');if(glow&&fine&&!reduce)addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'},{passive:true});if(fine&&!reduce){document.querySelectorAll('.visual').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top;card.style.setProperty('--rx',`${((y/r.height)-.5)*-3}deg`);card.style.setProperty('--ry',`${((x/r.width)-.5)*4}deg`);card.style.setProperty('--mx',`${x/r.width*100}%`);card.style.setProperty('--my',`${y/r.height*100}%`)});card.addEventListener('pointerleave',()=>{card.style.setProperty('--rx','0deg');card.style.setProperty('--ry','0deg')})});document.querySelectorAll('.magnetic').forEach(btn=>{btn.addEventListener('pointermove',e=>{const r=btn.getBoundingClientRect();btn.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.12}px,${(e.clientY-r.top-r.height/2)*.16}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')})}const counters=document.querySelectorAll('[data-count]');const countObs=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=Number(el.dataset.count),start=performance.now(),dur=900;function tick(now){const p=Math.min(1,(now-start)/dur),ease=1-Math.pow(1-p,3);el.textContent=Math.round(target*ease)+(target>=10?'+':'');if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick);countObs.unobserve(el)}),{threshold:.5});counters.forEach(c=>countObs.observe(c));if(!reduce)addEventListener('scroll',()=>{const y=scrollY;document.querySelectorAll('.a1').forEach(el=>el.style.marginTop=`${y*.025}px`);document.querySelectorAll('.a2').forEach(el=>el.style.marginTop=`${-y*.018}px`)},{passive:true});const pet=document.querySelector('.cursor-pet');if(pet&&fine&&!reduce){let tx=innerWidth*.82,ty=innerHeight*.72,x=tx,y=ty,last=performance.now(),idleTimer;const wake=()=>{pet.classList.add('awake','chasing');pet.classList.remove('napping');clearTimeout(idleTimer);idleTimer=setTimeout(()=>{pet.classList.remove('chasing');pet.classList.add('napping')},1800)};addEventListener('pointermove',e=>{tx=e.clientX+20;ty=e.clientY+22;wake()},{passive:true});document.querySelectorAll('.visual,.utility,.btn,.pill').forEach(el=>{el.addEventListener('pointerenter',()=>pet.classList.add('excited'));el.addEventListener('pointerleave',()=>pet.classList.remove('excited'))});function chase(now){const dt=Math.min(32,now-last);last=now;const ease=1-Math.pow(.82,dt/16.67);x+=(tx-x)*ease;y+=(ty-y)*ease;const dx=tx-x,dy=ty-y,angle=Math.max(-12,Math.min(12,Math.atan2(dy,Math.max(20,Math.abs(dx)))*180/Math.PI));pet.style.transform=`translate3d(${x}px,${y}px,0) rotate(${angle*.22}deg) scaleX(${dx<0?-1:1})`;requestAnimationFrame(chase)}pet.classList.add('awake');requestAnimationFrame(chase)}