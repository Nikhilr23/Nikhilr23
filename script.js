const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;requestAnimationFrame(()=>document.body.classList.add('hero-ready'));const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});reveals.forEach(el=>observer.observe(el));const progress=document.querySelector('.progress');addEventListener('scroll',()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max?scrollY/max*100:0}%`},{passive:true});const glow=document.querySelector('.cursor-glow');if(glow&&matchMedia('(pointer:fine)').matches&&!reduce)addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'},{passive:true});if(matchMedia('(pointer:fine)').matches&&!reduce){document.querySelectorAll('.visual').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top;card.style.setProperty('--rx',`${((y/r.height)-.5)*-3}deg`);card.style.setProperty('--ry',`${((x/r.width)-.5)*4}deg`);card.style.setProperty('--mx',`${x/r.width*100}%`);card.style.setProperty('--my',`${y/r.height*100}%`)});card.addEventListener('pointerleave',()=>{card.style.setProperty('--rx','0deg');card.style.setProperty('--ry','0deg')})});document.querySelectorAll('.magnetic').forEach(btn=>{btn.addEventListener('pointermove',e=>{const r=btn.getBoundingClientRect();btn.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.12}px,${(e.clientY-r.top-r.height/2)*.16}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')})}const counters=document.querySelectorAll('[data-count]');const countObs=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=Number(el.dataset.count),start=performance.now(),dur=900;function tick(now){const p=Math.min(1,(now-start)/dur),ease=1-Math.pow(1-p,3);el.textContent=Math.round(target*ease)+(target>=10?'+':'');if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick);countObs.unobserve(el)}),{threshold:.5});counters.forEach(c=>countObs.observe(c));if(!reduce){addEventListener('scroll',()=>{const y=scrollY;document.querySelectorAll('.a1').forEach(el=>el.style.marginTop=`${y*.025}px`);document.querySelectorAll('.a2').forEach(el=>el.style.marginTop=`${-y*.018}px`)},{passive:true})}