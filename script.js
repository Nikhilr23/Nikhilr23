(()=>{'use strict';const reduced=matchMedia('(prefers-reduced-motion:reduce)').matches,fine=matchMedia('(pointer:fine)').matches,root=document.documentElement,clamp=(n,a,b)=>Math.min(b,Math.max(a,n));addEventListener('load',()=>setTimeout(()=>document.querySelector('.loader')?.classList.add('done'),300),{once:true});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.08,rootMargin:'0px 0px -8%'});document.querySelectorAll('.reveal').forEach(el=>reduced?el.classList.add('visible'):io.observe(el));let queued=false;const paint=()=>{root.style.setProperty('--progress',clamp(scrollY/Math.max(1,root.scrollHeight-innerHeight),0,1));queued=false};addEventListener('scroll',()=>{if(!queued){queued=true;requestAnimationFrame(paint)}},{passive:true});paint();
if(fine&&!reduced){const dot=document.querySelector('.cursor'),ring=document.querySelector('.cursor-ring');let x=innerWidth/2,y=innerHeight/2,rx=x,ry=y;const draw=()=>{rx+=(x-rx)*.14;ry+=(y-ry)*.14;dot.style.transform=`translate(${x}px,${y}px) translate(-50%,-50%)`;ring.style.transform=`translate(${rx}px,${ry}px) translate(-50%,-50%)`;requestAnimationFrame(draw)};addEventListener('pointermove',e=>{x=e.clientX;y=e.clientY},{passive:true});draw();document.querySelectorAll('a,button').forEach(el=>{el.onpointerenter=()=>ring.classList.add('link');el.onpointerleave=()=>ring.classList.remove('link')});document.querySelectorAll('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.07}px,${(e.clientY-r.top-r.height/2)*.07}px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});document.querySelectorAll('[data-tilt]').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),px=(e.clientX-r.left)/r.width-.5,py=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(1400px) rotateX(${py*-1.2}deg) rotateY(${px*1.5}deg)`});el.addEventListener('pointerleave',()=>el.style.transform='')})}
if(!reduced){const c=document.querySelector('#field'),ctx=c.getContext('2d');let dots=[],w=0,h=0,dpr=1;const resize=()=>{dpr=Math.min(devicePixelRatio,2);w=innerWidth;h=innerHeight;c.width=w*dpr;c.height=h*dpr;c.style.width=w+'px';c.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);dots=Array.from({length:Math.min(65,Math.floor(w/20))},(_,i)=>({x:(i*97)%w,y:(i*53)%h,r:1+(i%3)*.3,v:.08+(i%5)*.015}))};const animate=()=>{ctx.clearRect(0,0,w,h);dots.forEach(d=>{d.y-=d.v;if(d.y<-4)d.y=h+4;ctx.fillStyle='#c9ff635c';ctx.beginPath();ctx.arc(d.x,d.y,d.r,0,Math.PI*2);ctx.fill()});requestAnimationFrame(animate)};resize();animate();addEventListener('resize',resize,{passive:true})}
const links=[...document.querySelectorAll('header nav a')],sections=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)links.forEach(a=>a.classList.toggle('current',a.hash==='#'+e.target.id))}),{rootMargin:'-20% 0px -65%'});document.querySelectorAll('section[id]').forEach(s=>sections.observe(s));const dialog=document.querySelector('dialog'),frame=dialog.querySelector('iframe');document.querySelectorAll('[data-reel]').forEach(b=>b.onclick=()=>{frame.src=frame.dataset.src;dialog.showModal()});dialog.querySelector('.close').onclick=()=>dialog.close();dialog.onclick=e=>{if(e.target===dialog)dialog.close()};dialog.onclose=()=>frame.src=''})();
